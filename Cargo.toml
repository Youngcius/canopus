[package]
name = "canopus"
version = "0.1.1"
edition = "2021"

[lib]
# The name must match the Python module name
name = "_accel"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }
numpy = "0.23"
ndarray = "0.16"
num-complex = "0.4"
